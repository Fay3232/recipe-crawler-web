{
  "name": "line-ai-assistant",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "LINE Messaging API webhook with OpenAI tool calling for food, stocks, and weather.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --env-file=.env src/server.js",
    "check": "node --check src/config.js && node --check src/fallback.js && node --check src/line.js && node --check src/tools.js && node --check src/providers/openai.js && node --check src/providers/gemini.js && node --check src/ai.js && node --check src/openai.js && node --check src/server.js",
    "test": "node --test"
  }
}
